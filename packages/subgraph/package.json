{
  "name": "@scaffold-eth/subgraph",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ GITHUB_USERNAME/your-contract",
    "create-local": "graph create --node http://localhost:8020/ unit4",
    "remove-local": "graph remove --node http://localhost:8020/ unit4",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 unit4"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0",
    "@types/node": "^16.7.2"
  },
  "devDependencies": {
    "mustache": "^3.1.0"
  }
}
